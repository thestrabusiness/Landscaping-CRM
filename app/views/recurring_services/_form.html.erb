<%= form_for(@recurring_service) do |f| %>
  <% if @recurring_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recurring_service.errors.count, "error") %> prohibited this recurring_service from being saved:</h2>

      <ul>
      <% @recurring_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
