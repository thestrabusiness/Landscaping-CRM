<% @invoices.each do |label| %>
  <div class="label">
    <%= label.client.first_name %> <%= label.client.last_name %> <br>
    <%= label.client.billing_address %> <br>
    <%= label.client.city %>, <%= label.client.state %> <%= "%05d" % label.client.zip %>  
  </div>
<% end %>