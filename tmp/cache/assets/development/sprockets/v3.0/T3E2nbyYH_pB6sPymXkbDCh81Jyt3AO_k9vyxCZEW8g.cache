I"m(function() {
  var ready;

  ready = function() {
    return $('#other').hide();
  };

  $('#service_name').change(function() {
    var name;
    name = $('#service_name option').filter(':selected').text();
    if (name === "Other") {
      return $('#other').show();
    }
  });

  $(document).ready(ready);

  $(document).on('page:load', ready);

}).call(this);
:ET