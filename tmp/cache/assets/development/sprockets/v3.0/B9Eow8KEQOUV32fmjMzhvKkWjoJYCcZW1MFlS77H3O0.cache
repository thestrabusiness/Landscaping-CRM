I"r(function() {
  var ready;

  ready = function() {
    return $('#other').hide();
  };

  ($('select#service_name')).change(function() {
    var name;
    name = ($('select#service_name option:selected')).text();
    if (name === "Other") {
      return $('#other').show();
    }
  });

  $(document).ready(ready);

  $(document).on('page:load', ready);

}).call(this);
:ET