I"~(function() {
  var ready;

  ready = function() {
    return $('#other').hide();
  };

  jQuery(function() {
    var name;
    ($('select#name')).change(function() {});
    name = ($('select#name option :selected')).text();
    if (name === "Other") {
      return ($('#other')).show;
    }
  });

  $(document).ready(ready);

  $(document).on('page:load', ready);

}).call(this);
:ET