I"^(function() {
  var ready;

  ready = function() {
    return $('#other').hide();
  };

  $('#_name').change(function() {
    var name;
    name = $('#name option').filter(':selected').text();
    if (name === "Other") {
      return $('#other').show();
    }
  });

  $(document).ready(ready);

  $(document).on('page:load', ready);

}).call(this);
:ET